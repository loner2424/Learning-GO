package main

import "fmt"

func constants() {

	const num int = 54
	const name string = "Sahil Sobhani"
	pi := 3.14

	fmt.Printf("Here the number: %d the string name: %s and the value pi: %f are all constants and their value cannot be changed", num, name, pi)

}
